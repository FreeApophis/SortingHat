/* Create TagsFiles */

CREATE TABLE IF NOT EXISTS `TagsFiles` (
	`ID`	INTEGER PRIMARY KEY AUTOINCREMENT,
	`TagID`	INTEGER NULL,
	`FileID`	INTEGER NOT NULL,
	FOREIGN KEY(`TagID`) REFERENCES `Tags`(`ID`),
	FOREIGN KEY(`FileID`) REFERENCES `Files`(`ID`)
);

